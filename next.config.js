/** @type {import('next').NextConfig} */
module.exports = {
  // Workaround (uncomment to fix): keep pino out of the server bundle
  // serverExternalPackages: ['pino', 'pino-pretty', 'thread-stream'],
}
