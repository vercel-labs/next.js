module.exports = {
  // Not required to reproduce CASE-7 (the console patch is installed in dev
  // regardless), but needed to see the forwarded logs in the terminal.
  logging: { browserToTerminal: true },
}
