/** @type {import("next").NextConfig} */
module.exports = {
  reactStrictMode: true,
  // Uncomment to work around the issue (must list the *origin* host, not the tunnel host):
  // experimental: { serverActions: { allowedOrigins: ["localhost:3000"] } },
};
