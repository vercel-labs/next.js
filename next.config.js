/** @type {import('next').NextConfig} */
module.exports = {
  // Uncomment to see the partial workaround: the Node.js instrumentation then
  // runs, but the Edge instrumentation compilation still fails.
  // serverExternalPackages: ['@napi-rs/tar'],
}
