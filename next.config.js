/** @type {import('next').NextConfig} */
// Uncomment to make /api/hello return 200 (pdfmake must stay unbundled):
// const nextConfig = { serverExternalPackages: ["pdfmake"] };
const nextConfig = {};
module.exports = nextConfig;
