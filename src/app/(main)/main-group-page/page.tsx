export default function MainGroupActualPage() {
  return (
    <div>
      <h1 className="text-lg font-bold">(Main) Group - Home Page</h1>
      <p>This is the main page within the (main) group, explicitly at <code>/main-group-page</code>.</p>
    </div>
  );
}
