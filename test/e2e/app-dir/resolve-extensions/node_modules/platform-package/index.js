// Extension-less import inside a third-party package: `resolveExtensions`
// ordering has to apply here too, so `./impl/impl.web.js` must win over
// `./impl/impl.js`.
export { platformName } from './impl/impl'
