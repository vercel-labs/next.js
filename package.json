{
  "name": "next-71515-custom-server-bundle",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "next build",
    "bundle:esbuild": "esbuild server.js --bundle --platform=node --log-limit=0 --log-level=error --outfile=out/server.js",
    "repro": "next build && npm run bundle:esbuild"
  },
  "dependencies": {
    "next": "16.3.1",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "devDependencies": {
    "esbuild": "0.24.0"
  }
}
