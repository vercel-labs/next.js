/** @type {import('next').NextConfig} */
const nextConfig = {
    /* config options here */
    reactStrictMode: true,
    i18n: {
        locales: ['en-US', 'pt-BR'],
        defaultLocale: 'en-US',
    },
};

export default nextConfig;
