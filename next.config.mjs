/** @type {import('next').NextConfig} */
const nextConfig = {
  // Uncomment to work around the error (Next.js then transpiles the package):
  // transpilePackages: ['duck-duck-scrape'],
}
export default nextConfig
