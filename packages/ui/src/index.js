import { translate } from "@repro/i18n";
import { Icon } from "@repro/icons";
import { cx } from "@repro/utils";
export const Button = ({ children }) => children;
export const label = (k) => translate(k);
export { Icon, cx };
