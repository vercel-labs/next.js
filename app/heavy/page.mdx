# Heavy math page

## Section 1

Inline $E_1 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{1}}{e^x-1}dx = \Gamma(1+1)\zeta(1+1) + \sum_{k=1}^{1} \binom{1}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 1 with **bold** and `code`.

## Section 2

Inline $E_2 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{2}}{e^x-1}dx = \Gamma(2+1)\zeta(2+1) + \sum_{k=1}^{2} \binom{2}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 2 with **bold** and `code`.

## Section 3

Inline $E_3 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{3}}{e^x-1}dx = \Gamma(3+1)\zeta(3+1) + \sum_{k=1}^{3} \binom{3}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 3 with **bold** and `code`.

## Section 4

Inline $E_4 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{4}}{e^x-1}dx = \Gamma(4+1)\zeta(4+1) + \sum_{k=1}^{4} \binom{4}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 4 with **bold** and `code`.

## Section 5

Inline $E_5 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{5}}{e^x-1}dx = \Gamma(5+1)\zeta(5+1) + \sum_{k=1}^{5} \binom{5}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 5 with **bold** and `code`.

## Section 6

Inline $E_6 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{6}}{e^x-1}dx = \Gamma(6+1)\zeta(6+1) + \sum_{k=1}^{6} \binom{6}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 6 with **bold** and `code`.

## Section 7

Inline $E_7 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{7}}{e^x-1}dx = \Gamma(7+1)\zeta(7+1) + \sum_{k=1}^{7} \binom{7}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 7 with **bold** and `code`.

## Section 8

Inline $E_8 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{8}}{e^x-1}dx = \Gamma(8+1)\zeta(8+1) + \sum_{k=1}^{8} \binom{8}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 8 with **bold** and `code`.

## Section 9

Inline $E_9 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{9}}{e^x-1}dx = \Gamma(9+1)\zeta(9+1) + \sum_{k=1}^{9} \binom{9}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 9 with **bold** and `code`.

## Section 10

Inline $E_10 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{10}}{e^x-1}dx = \Gamma(10+1)\zeta(10+1) + \sum_{k=1}^{10} \binom{10}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 10 with **bold** and `code`.

## Section 11

Inline $E_11 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{11}}{e^x-1}dx = \Gamma(11+1)\zeta(11+1) + \sum_{k=1}^{11} \binom{11}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 11 with **bold** and `code`.

## Section 12

Inline $E_12 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{12}}{e^x-1}dx = \Gamma(12+1)\zeta(12+1) + \sum_{k=1}^{12} \binom{12}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 12 with **bold** and `code`.

## Section 13

Inline $E_13 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{13}}{e^x-1}dx = \Gamma(13+1)\zeta(13+1) + \sum_{k=1}^{13} \binom{13}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 13 with **bold** and `code`.

## Section 14

Inline $E_14 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{14}}{e^x-1}dx = \Gamma(14+1)\zeta(14+1) + \sum_{k=1}^{14} \binom{14}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 14 with **bold** and `code`.

## Section 15

Inline $E_15 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{15}}{e^x-1}dx = \Gamma(15+1)\zeta(15+1) + \sum_{k=1}^{15} \binom{15}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 15 with **bold** and `code`.

## Section 16

Inline $E_16 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{16}}{e^x-1}dx = \Gamma(16+1)\zeta(16+1) + \sum_{k=1}^{16} \binom{16}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 16 with **bold** and `code`.

## Section 17

Inline $E_17 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{17}}{e^x-1}dx = \Gamma(17+1)\zeta(17+1) + \sum_{k=1}^{17} \binom{17}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 17 with **bold** and `code`.

## Section 18

Inline $E_18 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{18}}{e^x-1}dx = \Gamma(18+1)\zeta(18+1) + \sum_{k=1}^{18} \binom{18}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 18 with **bold** and `code`.

## Section 19

Inline $E_19 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{19}}{e^x-1}dx = \Gamma(19+1)\zeta(19+1) + \sum_{k=1}^{19} \binom{19}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 19 with **bold** and `code`.

## Section 20

Inline $E_20 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{20}}{e^x-1}dx = \Gamma(20+1)\zeta(20+1) + \sum_{k=1}^{20} \binom{20}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 20 with **bold** and `code`.

## Section 21

Inline $E_21 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{21}}{e^x-1}dx = \Gamma(21+1)\zeta(21+1) + \sum_{k=1}^{21} \binom{21}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 21 with **bold** and `code`.

## Section 22

Inline $E_22 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{22}}{e^x-1}dx = \Gamma(22+1)\zeta(22+1) + \sum_{k=1}^{22} \binom{22}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 22 with **bold** and `code`.

## Section 23

Inline $E_23 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{23}}{e^x-1}dx = \Gamma(23+1)\zeta(23+1) + \sum_{k=1}^{23} \binom{23}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 23 with **bold** and `code`.

## Section 24

Inline $E_24 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{24}}{e^x-1}dx = \Gamma(24+1)\zeta(24+1) + \sum_{k=1}^{24} \binom{24}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 24 with **bold** and `code`.

## Section 25

Inline $E_25 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{25}}{e^x-1}dx = \Gamma(25+1)\zeta(25+1) + \sum_{k=1}^{25} \binom{25}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 25 with **bold** and `code`.

## Section 26

Inline $E_26 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{26}}{e^x-1}dx = \Gamma(26+1)\zeta(26+1) + \sum_{k=1}^{26} \binom{26}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 26 with **bold** and `code`.

## Section 27

Inline $E_27 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{27}}{e^x-1}dx = \Gamma(27+1)\zeta(27+1) + \sum_{k=1}^{27} \binom{27}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 27 with **bold** and `code`.

## Section 28

Inline $E_28 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{28}}{e^x-1}dx = \Gamma(28+1)\zeta(28+1) + \sum_{k=1}^{28} \binom{28}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 28 with **bold** and `code`.

## Section 29

Inline $E_29 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{29}}{e^x-1}dx = \Gamma(29+1)\zeta(29+1) + \sum_{k=1}^{29} \binom{29}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 29 with **bold** and `code`.

## Section 30

Inline $E_30 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{30}}{e^x-1}dx = \Gamma(30+1)\zeta(30+1) + \sum_{k=1}^{30} \binom{30}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 30 with **bold** and `code`.

## Section 31

Inline $E_31 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{31}}{e^x-1}dx = \Gamma(31+1)\zeta(31+1) + \sum_{k=1}^{31} \binom{31}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 31 with **bold** and `code`.

## Section 32

Inline $E_32 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{32}}{e^x-1}dx = \Gamma(32+1)\zeta(32+1) + \sum_{k=1}^{32} \binom{32}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 32 with **bold** and `code`.

## Section 33

Inline $E_33 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{33}}{e^x-1}dx = \Gamma(33+1)\zeta(33+1) + \sum_{k=1}^{33} \binom{33}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 33 with **bold** and `code`.

## Section 34

Inline $E_34 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{34}}{e^x-1}dx = \Gamma(34+1)\zeta(34+1) + \sum_{k=1}^{34} \binom{34}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 34 with **bold** and `code`.

## Section 35

Inline $E_35 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{35}}{e^x-1}dx = \Gamma(35+1)\zeta(35+1) + \sum_{k=1}^{35} \binom{35}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 35 with **bold** and `code`.

## Section 36

Inline $E_36 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{36}}{e^x-1}dx = \Gamma(36+1)\zeta(36+1) + \sum_{k=1}^{36} \binom{36}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 36 with **bold** and `code`.

## Section 37

Inline $E_37 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{37}}{e^x-1}dx = \Gamma(37+1)\zeta(37+1) + \sum_{k=1}^{37} \binom{37}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 37 with **bold** and `code`.

## Section 38

Inline $E_38 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{38}}{e^x-1}dx = \Gamma(38+1)\zeta(38+1) + \sum_{k=1}^{38} \binom{38}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 38 with **bold** and `code`.

## Section 39

Inline $E_39 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{39}}{e^x-1}dx = \Gamma(39+1)\zeta(39+1) + \sum_{k=1}^{39} \binom{39}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 39 with **bold** and `code`.

## Section 40

Inline $E_40 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{40}}{e^x-1}dx = \Gamma(40+1)\zeta(40+1) + \sum_{k=1}^{40} \binom{40}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 40 with **bold** and `code`.

## Section 41

Inline $E_41 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{41}}{e^x-1}dx = \Gamma(41+1)\zeta(41+1) + \sum_{k=1}^{41} \binom{41}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 41 with **bold** and `code`.

## Section 42

Inline $E_42 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{42}}{e^x-1}dx = \Gamma(42+1)\zeta(42+1) + \sum_{k=1}^{42} \binom{42}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 42 with **bold** and `code`.

## Section 43

Inline $E_43 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{43}}{e^x-1}dx = \Gamma(43+1)\zeta(43+1) + \sum_{k=1}^{43} \binom{43}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 43 with **bold** and `code`.

## Section 44

Inline $E_44 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{44}}{e^x-1}dx = \Gamma(44+1)\zeta(44+1) + \sum_{k=1}^{44} \binom{44}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 44 with **bold** and `code`.

## Section 45

Inline $E_45 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{45}}{e^x-1}dx = \Gamma(45+1)\zeta(45+1) + \sum_{k=1}^{45} \binom{45}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 45 with **bold** and `code`.

## Section 46

Inline $E_46 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{46}}{e^x-1}dx = \Gamma(46+1)\zeta(46+1) + \sum_{k=1}^{46} \binom{46}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 46 with **bold** and `code`.

## Section 47

Inline $E_47 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{47}}{e^x-1}dx = \Gamma(47+1)\zeta(47+1) + \sum_{k=1}^{47} \binom{47}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 47 with **bold** and `code`.

## Section 48

Inline $E_48 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{48}}{e^x-1}dx = \Gamma(48+1)\zeta(48+1) + \sum_{k=1}^{48} \binom{48}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 48 with **bold** and `code`.

## Section 49

Inline $E_49 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{49}}{e^x-1}dx = \Gamma(49+1)\zeta(49+1) + \sum_{k=1}^{49} \binom{49}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 49 with **bold** and `code`.

## Section 50

Inline $E_50 = mc^2$ and block:

$$
\int_0^{\infty} \frac{x^{50}}{e^x-1}dx = \Gamma(50+1)\zeta(50+1) + \sum_{k=1}^{50} \binom{50}{k} \alpha_k \beta_{k}^{2}
$$

Some prose paragraph number 50 with **bold** and `code`.

