import { Heavy } from '../components/Heavy';
import { Lazy } from '../components/Lazy';

export default function Page() {
    return (
        <main>
            <Heavy />
            <Lazy />
        </main>
    );
}
